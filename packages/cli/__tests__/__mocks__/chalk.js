module.exports = {
  red: (str) => str,
  green: (str) => str,
  blue: (str) => str,
  yellow: (str) => str,
  magenta: (str) => str,
  cyan: (str) => str,
  white: (str) => str,
  gray: (str) => str,
  bold: (str) => str,
  default: {
    red: (str) => str,
    green: (str) => str,
    blue: (str) => str,
    yellow: (str) => str,
    magenta: (str) => str,
    cyan: (str) => str,
    white: (str) => str,
    gray: (str) => str,
    bold: (str) => str,
  }
};
