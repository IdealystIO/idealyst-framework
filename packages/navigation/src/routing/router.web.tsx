import { NavigatorParam } from './types'

import { Routes, Route, Outlet, useLocation, useNavigate } from "react-router-dom";

export const buildNavigator = (params: NavigatorParam) => {
    return (
        <Routes>

        </Routes>
    )
}