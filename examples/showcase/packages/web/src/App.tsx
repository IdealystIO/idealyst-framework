import { App } from '@idealyst-showcase/shared';
import { BrowserRouter } from 'react-router-dom';

function WebApp() {
  return (
    <BrowserRouter>
      <App />
    </BrowserRouter>
  );
}

export default WebApp;
